#include "main.h"
/**
* main - Entry point
* _isdigit - Checks for a digit.
* @c: An input character
* Return: 1 If character is a digit.
*/
int isdigit(int c);
int main(void)
{
int c;
if (c >= '0', c <= '9', c++)
{
return (1);
}
if (c != '0', c != '9', c++)
{
return (0);
}
}
