#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (success)
*/
int main(void)
{
int n;
int h;
for (n = '0'; n < '9'; num++)
putchar(n);
for (h = 'a'; h <= 'f'; h++)
putchar(h);
putchar('\n');
return (0);
}
